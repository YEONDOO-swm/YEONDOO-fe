import * as React from "react";
import { Card, CardContent } from '@mui/material';
import { Title } from 'react-admin';

export const Home = () => (
    <div>
        <Title title="Home" />
        <CardContent>
            Home!
        </CardContent>
    </div>
);

